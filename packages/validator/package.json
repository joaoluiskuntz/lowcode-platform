{
    "name": "@lowcode/validator",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@lowcode/dsl-schema": "1.0.0",
        "ajv": "^8.18.0",
        "ajv-formats": "^3.0.1"
    }
}
